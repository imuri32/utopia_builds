<!DOCTYPE html>
<html>
  <head>
    <title>Utopia Builds</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  </head>

  <body>
    <nav class="navbar fixed-top navbar-expand-lg navbar-light bg-light" style="color: white;">
      <div class="container">
        <ul class="navbar-nav">
          <li class="nav-item">
            <%= link_to "Utopia Builds", root_path, class: "navbar-brand", style: "font-size: 30px;" %>
          </li>
          <li class="nav-item pt-2">
            <%= link_to "Brands", brands_path, class: "nav-link" %>
          </li>
          <li class="nav-item pt-2">
            <%= link_to "Contact", contact_path, class: "nav-link" %>
          </li>
          <li class="nav-item pt-2">
            <%= link_to "About Us", about_path, class: "nav-link" %>
          </li>
        </ul>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNavAltMarkup">
          <div class="navbar-nav d-flex align-items-center mr-4">
            <% if user_signed_in? %>
              <div class="nav-item">
                <%= link_to "Edit Profile", edit_user_registration_path, class: "nav-link" %>
              </div>
              <div class="nav-item">
                <%= link_to "My Orders", orders_path, class: "nav-link" %>
              </div>
              <div class="nav-item">
                <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "nav-link" %>
              </div>
            <% else %>
              <div class="nav-item">
                <%= link_to "Sign In", new_user_session_path, class: "nav-link" %>
              </div>
              <div class="nav-item">
                <%= link_to "Sign Up", new_user_registration_path, class: "nav-link" %>
              </div>
            <% end %>
          </div>
          <div class="navbar-nav">
            <%= link_to checkout_path, style: "text-decoration: none;" do %>
              <span class="navbar-brand">Cart</span>
              <span class="navbar-brand border-0 rounded" style="min-width: 40px; text-align: center; background-color: black; color: white;"><%= session[:shopping_cart].size %></span>
            <% end %>
          </div>
        </div>
      </div>
    </nav>
    <div class="container" style="padding-top: 125px;">
      <% if notice %>
        <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-danger"><%= alert %></p>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
